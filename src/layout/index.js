import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const Layout = () => {
  return (
    <BrowserRouter>
      {/* Must be Header and Body */}
    </BrowserRouter>
  );
}

export default Layout;
